#include <iostream>

using namespace std;

int main() {
    //Variables to store data
    int num = 3;

    // Logic to check even or odd number using if condition
    if (num % 2 == 0) {
        cout << num << " is Even" << endl;
    } else {
        cout << num << " is Odd";
    }

    return 0;
}